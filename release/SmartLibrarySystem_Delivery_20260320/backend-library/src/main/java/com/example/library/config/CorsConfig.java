package com.example.library.config;

/**
 * CorsConfig removed — CORS is now configured solely in SecurityConfig.
 * This avoids duplicate CORS configuration that could lead to maintenance issues.
 *
 * @see com.example.library.security.SecurityConfig#corsConfigurationSource()
 */
// Class intentionally left empty — preserved to avoid breaking Spring component scans
// that may reference this package. Original CorsFilter bean has been removed.
